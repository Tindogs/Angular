import { DogToMatchDirective } from './dog-to-match.directive';

describe('DogToMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new DogToMatchDirective();
    expect(directive).toBeTruthy();
  });
});
